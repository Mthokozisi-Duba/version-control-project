# Mthokozisi Duba u24690059

version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - MONGODB_URI=mongodb+srv://test-user:test-password@test-user.4vvncnb.mongodb.net/?retryWrites=true&w=majority&appName=test-user
    restart: unless-stopped
